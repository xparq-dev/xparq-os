# Legacy Windows Helpers

Scripts in this directory are historical debugging and prototype workflows. They are non-canonical and must not publish release artifacts or claim runtime verification.

Use `tools/build-x86_64.ps1` for supported x86-64 builds, image validation, manifest generation, and QEMU smoke tests.
